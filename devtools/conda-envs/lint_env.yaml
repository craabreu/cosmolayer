name: linter
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - periodictable
  - pyyaml
  - typing-extensions

    # Testing
  - matplotlib
  - pytest
  - pytest-cov

    # Machine learning dependencies
  - pytorch
  - lightning
  - opt-einsum
