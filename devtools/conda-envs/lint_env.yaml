name: linter
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends
  - python
  - numpy
  - pandas
  - periodictable
  - pyyaml
  - scipy

    # Testing
  - pytest

    # Linting
  - ruff
  - mypy
