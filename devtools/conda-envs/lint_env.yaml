name: linter
channels:
  - conda-forge
  - mdtools
dependencies:
    # Base depends (from pyproject.toml)
  - python
  - lightning
  - numpy
  - opt-einsum
  - pandas
  - periodictable
  - pyyaml
  - scipy
  - torch

    # Testing
  - pytest

    # Linting
  - ruff
  - mypy
